# UiOne AI configuration. Copy to .env and adjust.
# Nothing here may point at the public internet — this is an on-premise product.

UIONE_HOST=127.0.0.1
UIONE_PORT=8000
UIONE_LOG_LEVEL=INFO
UIONE_ENVIRONMENT=dev

# Model plane: any OpenAI-compatible endpoint.
#   production  -> llm_inference_engine (vLLM adapter)
#   development -> llm_inference_engine (llama.cpp/MLX adapter) or Ollama
UIONE_MODEL_PLANE_URL=http://127.0.0.1:8080/v1
UIONE_MODEL_PLANE_API_KEY=

# Task tiers — see docs/PRODUCT_STRATEGY_AND_BACKLOG.md Appendix A for the
# production portfolio, and docs/MODEL_TRIALS.md for measured tool-calling results.
UIONE_MODEL_TIER_TRIAGE=qwen3:4b
UIONE_MODEL_TIER_WORKHORSE=qwen3:30b-a3b
UIONE_MODEL_TIER_REASONING=qwen3:30b-a3b
UIONE_MODEL_TIER_EMBEDDING=qwen3-embedding:0.6b

# Local development profile against Ollama. Every model below scored 4/4 on the
# tool-calling probes in docs/MODEL_TRIALS.md.
#   UIONE_MODEL_PLANE_URL=http://127.0.0.1:11434/v1
#   UIONE_MODEL_TIER_TRIAGE=ministral-3:3b
#   UIONE_MODEL_TIER_WORKHORSE=ministral-3:8b
#   UIONE_MODEL_TIER_REASONING=qwen3.6:27b

UIONE_DATABASE_URL=sqlite+aiosqlite:///./uione.db

# preview | approve | auto — deny-by-default autonomy ladder (gap G1)
UIONE_AUTONOMY_DEFAULT_MODE=preview

# --- Mail connector ---
# Leave UIONE_MAIL_IMAP_HOST empty to use the fixture connector (no infrastructure
# needed). Set it to switch to real IMAP/SMTP. See docs/CONNECTORS.md.
# UIONE_MAIL_IMAP_HOST=mail.corp.example
# UIONE_MAIL_IMAP_PORT=993
# UIONE_MAIL_USERNAME=alice@corp.example
# UIONE_MAIL_PASSWORD=            # from the secrets manager, never a prompt
# UIONE_MAIL_SMTP_HOST=smtp.corp.example

# Drives external-sender detection AND the outbound egress allowlist.
# Empty means every address is external, so outbound mail is refused rather
# than quietly allowed anywhere.
# UIONE_INTERNAL_DOMAINS=corp.example,corp.local

# Project key prefixes for the work graph, e.g. PAY,OPS. Known prefixes keep
# entity extraction precise; a generic ABC-123 pattern also matches ISO-9001.
# INC (incidents) and INV/CLM/PO (references) are recognised by default.
# UIONE_TICKET_PREFIXES=PAY,OPS

# --- Proactive engine ---
# UIONE_BRIEF_TIME=07:30
# UIONE_BRIEF_TIMEZONE=Europe/Istanbul
# UIONE_BRIEF_JITTER_S=900          # spread users so a fleet does not stampede
# UIONE_BRIEF_MAX_AGE_MINUTES=720   # withhold briefs older than this
# UIONE_SCHEDULER_CONCURRENCY=2     # background work yields to interactive requests
